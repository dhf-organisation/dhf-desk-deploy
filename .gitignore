node_modules/
.netlify/
.DS_Store
*.log
dist/

# Local Supabase dev stack — schema and dumps pulled from production must
# never be committed (RLS policies + function bodies stay out of this public
# repo until #6 — public/private decision — is resolved). See
# scripts/db-pull-schema.mjs and docs/environments.md.
supabase/.local-schema.sql
supabase/.env

# Personal Claude Code permission overrides — machine-local, never shared.
.claude/settings.local.json

# Playwright E2E output — see tests/e2e/README.md. node_modules/ above
# already covers tests/e2e/node_modules/.
tests/e2e/playwright-report/
tests/e2e/test-results/
tests/e2e/.auth/
